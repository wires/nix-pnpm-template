{
  "name": "nix-pnpm",
  "version": "1.0.0",
  "description": "To get a shell with just `pnpm` in it, run `nix-shell -p nodePackges.pnpm`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nanoid": "^3.1.25"
  }
}
